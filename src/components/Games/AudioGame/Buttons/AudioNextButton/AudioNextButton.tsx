import React from 'react';
import classes from './audioNextButton.module.scss';

const NextButton = () => {
  return (
    <div>
      <button className={`${'material-icons'} ${classes.audio_next_btn}`}> east</button>
    </div>
  )
};

export default NextButton;
